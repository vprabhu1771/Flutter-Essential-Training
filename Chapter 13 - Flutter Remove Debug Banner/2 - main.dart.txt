import 'package:flutter/material.dart';
import 'package:flutter_independent_music_streaming/screen/GenreScreen.dart';
import 'package:flutter_independent_music_streaming/screen/PlaylistScreen.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      initialRoute: '/',
      routes: {
        // '/': (context) => GenreScreen(title: 'Genre'),
        '/': (context) => PlaylistScreen(title: 'Playlist'),
      },
    );
  }
}
